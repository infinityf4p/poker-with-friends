{
  "name": "@poker-with-friends/engine",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:property": "vitest run src/engine.property.test.ts"
  },
  "devDependencies": {
    "fast-check": "^4.2.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
